# Used only by a future backend/serverless proxy (see README > "Connecting a real
# sports-data API"). The frontend never reads these directly and no secret key
# should ever be shipped to the browser.
SPORTS_API_KEY=
SPORTS_API_URL=
